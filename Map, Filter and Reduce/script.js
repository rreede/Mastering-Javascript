"use strict";

const arrayItems = ["Cat", "Dog", "Mouse"];

const payRoll = [2000, 1700, 1640, 1200];

const arrayMapped = arrayItems.map(function (animal) {
  console.log(`${animal} is a Pet`);
});

const payRollFilter = payRoll.filter;
